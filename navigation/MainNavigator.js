import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';

const MainStack = createStackNavigator<MainStackNavigatorParamsList>();

const MainNavigator = () => {
  return (
      <MainStack.Navigator>

      </MainStack.Navigator>
  );
};

export default MainNavigator;
